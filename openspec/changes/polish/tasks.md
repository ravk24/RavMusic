## 1. Settings data

- [ ] 1.1 Add `androidx.datastore:datastore-preferences` 1.1.7 to `gradle/libs.versions.toml` and `app/build.gradle.kts`; create `data/settings/ThemeMode.kt` (`SYSTEM`/`LIGHT`/`DARK`, `resolve(systemDark)`) and `data/settings/SettingsRepository.kt` (`themeMode`, `minDurationMs` flows with defaults, `setThemeMode`, `setMinDuration`); verify `ThemeModeTest` (resolve matrix) and `SettingsRepositoryTest` (defaults, round-trip, values survive re-opening the same temp file) pass and `:app:dependencies` still resolves kotlinx-coroutines 1.9.0
- [ ] 1.2 Change `MediaScanner` to `scan(minDurationMs)`, drop the constructor parameter from `MediaStoreScanner`, give `LibraryRepository` a `suspend () -> Long` threshold provider read inside `refresh()`, record `minDurationMs` on `LibrarySnapshot`, add `LibraryViewModel.refreshIfLoaded()`, and wire `AppContainer` (`dataStore`, `settingsRepository`, provider); update every `MediaScanner { }` fake; verify `LibraryRepositoryTest` additions (provider value reaches the scanner; snapshot records it; a second refresh uses the new value) and `LibraryViewModelTest` (`refreshIfLoaded` is a no-op while Idle/Loading, refreshes when Loaded) pass
- [ ] 1.3 Create `SettingsViewModel.kt` with `SettingsHost`, `SettingsViewModel(settingsRepository, libraryRepository)` and `NoSettings`; verify `SettingsViewModelTest` (setThemeMode persists; setMinDuration persists then triggers exactly one library refresh) passes

## 2. Settings UI and theme

- [ ] 2.1 Add `formatScanTime(scannedAt, now)` ("just now", "N min ago", "N h ago", else the date) and `thresholdLabel(ms)` ("Off", "15s", "30s", "1 min", "2 min") to `ui/components/Format.kt`; rewrite `ui/settings/SettingsScreen.kt` (Theme segmented buttons `theme_system`/`theme_light`/`theme_dark`; Skip short audio segmented buttons `min_duration_<ms>`; info line `settings_last_scan` reading "Last scan · <time> · N songs" or "Library not scanned yet"; `settings_rescan` button with a progress indicator while refreshing and disabled while not Loaded; footer `settings_footer` kept); verify `FormatTest` additions and a new `SettingsScreenTest` with `FakeSettingsHost` (selected states reflect the host; taps call `setThemeMode` / `setMinDuration`; info line for Idle / Loaded / refreshing; Rescan calls back and is disabled while Idle) pass
- [ ] 2.2 In `MainActivity` collect `themeMode`, resolve it against `isSystemInDarkTheme()`, pass it to `RavMusicTheme`, and re-invoke `enableEdgeToEdge` with matching `SystemBarStyle`s in a `LaunchedEffect`; build `SettingsViewModel` in `AppRoot`; thread `settings: SettingsHost` through `AppNavigation` (update every call site with `FakeSettingsHost`/`NoSettings`); make the Folders footer and empty-library hint read `snapshot.minDurationMs` ("audio under 15s hidden", omitted when Off); verify `FoldersScreenTest` additions (footer/hint per threshold) and the existing navigation tests pass

## 3. Missing files and empty states

- [ ] 3.1 Call `refreshIfLoaded()` from the existing `ON_RESUME` effect in `AppRoot`; create `playback/PlaybackEvents.kt`, broadcast `SKIPPED_MISSING` with the title from `MissingFileSkipper` (which now receives the session), map it in `PlayerConnection`'s `onCustomCommand` to `PlayerState.skipped: SkipNotice?(title, seq)`, and show it once per `seq` from a `SnackbarHost` on `AppNavigation`'s `Scaffold` ("Couldn't play <title> — skipped"); verify `PlaybackServiceTest` addition (a bad URI queued between two tones: the controller observes `skipped` naming it and item 2 plays) and `NowPlayingNavigationTest` addition (a state with `skipped` shows the snackbar text once) pass
- [ ] 3.2 Make `EmptyState.actionLabel` optional; replace the plain-text empties in `FolderDetailScreen` ("Nothing here yet", "Back to folders" → `onBack`) and `PlaylistDetailScreen` ("No songs yet", "Open Folders" → new `onOpenFolders`, wired in `AppNavigation` to pop the detail and select the Folders tab); keep test tags `folder_detail_empty` / `playlist_empty`; verify `FolderDetailScreenTest` and `PlaylistDetailScreenTest` empty cases (title, action callback) and a `LibraryNavigationTest` addition ("Open Folders" from an empty playlist lands on the Folders tab) pass

## 4. Motion

- [ ] 4.1 Create `ui/navigation/NavTransitions.kt` (default fade + horizontal slide specs, Now Playing vertical specs with `targetContentZIndex = 1f`), pass `transitionSpec` / `popTransitionSpec` / `predictivePopTransitionSpec` to `NavDisplay`, attach the Now Playing metadata to its entry, and add `android:enableOnBackInvokedCallback="true"` to the manifest; verify a new `NavTransitionTest` (with `mainClock.autoAdvance = false`: after the mini player tap both `screen_now_playing` and the tab screen exist; after advancing past the transition only the player remains; after collapse and settling only the tab remains) and all existing navigation tests pass
- [ ] 4.2 Wrap the `FolderDetailScreen` top bar in `AnimatedContent(targetState = selecting)` with a vertical slide + fade; verify `FolderDetailSelectionTest` still passes unchanged

## 5. Drag-select

- [ ] 5.1 Create `ui/folders/RangeSelection.kt` with `RowBounds`, `rowIndexAt(y, rows)` and `rangeSelection(base, ids, anchor, current)`; verify `RangeSelectionTest` (downward and upward ranges, retreat shrinks, base preserved, `rowIndexAt` inside / between / outside rows) passes
- [ ] 5.2 Add the list-level `pointerInput` drag-select gesture to `FolderDetailScreen` (long-press anchors, moves in the Initial pass update the selection through `rangeSelection`, up ends the gesture) while keeping the row `combinedClickable`; verify a new `FolderDetailDragSelectTest` (`performTouchInput { down(row1); advanceEventTime(longPress + margin); moveTo(row3) }` → "3 selected"; `moveTo(row2)` → "2 selected"; with row 5 pre-selected the drag yields "3 selected"; a plain long-press still yields "1 selected") passes on API 36 and API 26

## 6. Integration and docs

- [ ] 6.1 Run `.\gradlew.bat assembleDebug testDebugUnitTest` and `connectedDebugAndroidTest` on the API 36 and API 26 emulators; verify `BUILD SUCCESSFUL`, every test passes, merged manifest still 0 hits for `INTERNET`
- [ ] 6.2 Manual walkthrough on both emulators: Dark override on a light system (palette + light status icons) → `am force-stop` → relaunch still dark; threshold Off reveals `short clip`, 2 min hides `gamma`, Folders footer/hint follow; move a WAV away on the device, rescan the volume, Home and return → row gone from the folder, playlist row greyed; play a playlist containing the missing file → snackbar; empty folder and empty playlist states with their actions; mini player → Now Playing slides up, back slides down (predictive back on API 36); long-press + drag selects a range; screenshots of Settings (light/dark), empty states, drag-select; note deviations here
- [ ] 6.3 Update `Build-plan/README.md` (row 7 ✅), `Build-plan/phases/07-polish.md` (status, result, task progress) and `Build-plan/decisions.md` (D-47…); commit on `main` and push; verify `git status` is clean and `git log -1` shows the commit
